INPUT FUNCTIONS
@INPUT "User input"
@FILE start_dir prefix
@DIRECTORY start_dir
@CHOICE ["choice1", "choice2"]

CHECKS
@CHECKFORFILE file
@CHECKINFILE file "string"
@CHECKOUTPUT [command syntax] "output"
@CHECKOUTPUTLINE int [command syntax] "output"
@CHECKPACKAGE package_name

@METADATA @@ # Follows YAML syntax
name: "ScriptName"
description: "Description of what the script does"
dependencies:
  - package1
  - package2
root: true
@@

@INIT @@
INPUT FUNCTIONS
@@

@INSTALL @@ # ALSO NEEDS @REMOVE
bash code
@@

@CHECKINSTALL @@
CHECKS
@@

@REMOVE @@ # ALSO NEEDS @INSTALL
bash code
@@

@CHECKREMOVE @@
CHECKS
@@

@UPDATE @@ # Optional but recommended
bash code
@@

@RUN @@  #cannot be used in the same file as @INSTALL or @REMOVE
bash code
@@

@CHECKRUN @@
bash code
@@
