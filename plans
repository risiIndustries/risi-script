[X] Make Checks bash based
[X] Implement easy checks via bash

[X] Available actions, instead of set as flags.
[X] Make actions based on checks

[ ] Script verification

[X] Remove gsettings
[X] Remove run and installation mode

[ ] Redo GUI


